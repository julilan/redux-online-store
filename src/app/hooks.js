import { useDispatch, useSelector } from 'react-redux';

// Use throughout application instead plain `useDispatch` and `useSeletor`
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;
